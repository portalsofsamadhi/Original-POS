# Development environment
# Secrets live in .env.local (gitignored) and server/.env (gitignored)
# Leave VITE_API_URL empty to route /api/* through Vite proxy -> localhost:10000
VITE_API_URL=
VITE_APP_URL=http://localhost:3002