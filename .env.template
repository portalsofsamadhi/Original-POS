# Production Environment Variables Template
# Copy this file to .env and update with your production values

# PayPal Configuration (LIVE)
VITE_PAYPAL_CLIENT_ID=your_live_paypal_client_id_here

# Stripe Configuration (LIVE)
VITE_STRIPE_PUBLIC_KEY=your_live_stripe_public_key_here

# Newsletter API URL (DreamHost deployment)
VITE_NEWSLETTER_API_URL=https://yourdomain.com

# For DreamHost shared hosting, newsletter backend uses PHP + MySQL
# Update the PHP files in php-backend/ with your database credentials

# Email Configuration
TEAM_EMAIL=info@portalsofsamadhi.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_smtp_user@yourdomain.com
SMTP_PASS=your_smtp_app_password
SMTP_FROM="Portals of Samadhi <info@portalsofsamadhi.com>"
